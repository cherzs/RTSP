services:
  - type: web
    name: rtsp-streamer-backend
    env: python
    plan: starter
    buildCommand: "./build.sh"
    startCommand: "uvicorn rtsp_streamer.asgi:application --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: "rtsp_streamer.production_settings"
      - key: PYTHON_VERSION
        value: "3.11"
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "*"
      - key: DATABASE_URL
        value: "postgresql://postgres:SntwlimWMUXtBNCWOQUNVnECjPKptUop@yamabiko.proxy.rlwy.net:44506/railway"
      # Redis temporarily disabled - using InMemory fallback
      # - key: REDIS_URL
      #   fromService:
      #     type: redis
      #     name: rtsp-redis
      #     property: connectionString
  
  # Redis service temporarily disabled
  # - type: redis
  #   name: rtsp-redis
  #   plan: starter